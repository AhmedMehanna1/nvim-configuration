return {
    -- "akinsho/toggleterm.nvim",
    -- config = function()
    --     require("toggleterm").setup({
    --         size = 20, -- Height of the terminal
    --         open_mapping = "T", -- Keybinding to toggle the terminal
    --         direction = "float", -- Options: 'horizontal', 'vertical', 'float'
    --         shade_terminals = true, -- Add a slight background shade
    --     })
    -- end,
}
